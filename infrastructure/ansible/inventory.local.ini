# Run this playbook from an SSM shell on the EC2 instance.
# The bootstrap script clones the repository, then Ansible configures that host
# locally. No SSH port, private key, or long-lived AWS credential is needed.
[self_healing_hosts]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
