# Recovery runs against the local machine (the webhook container itself),
# which reaches the NGINX container over the Docker socket + Compose network.
# On Day 5 this gets replaced by a real EC2 inventory reached over SSH/SSM.
[recovery_targets]
localhost ansible_connection=local
